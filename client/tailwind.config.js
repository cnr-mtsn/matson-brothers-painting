module.exports = {
	purge: [],
	darkMode: process.env.NODE_ENV === "development" ? "class" : "media", // or 'media' or 'class'
	theme: {
		extend: {},
	},
	variants: {
		extend: {},
	},
	plugins: [],
}
